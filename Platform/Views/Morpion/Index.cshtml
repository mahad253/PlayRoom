@{
    ViewData["Title"] = "Morpion";
    var error = TempData["Error"] as string;
}

<section class="morpion-page">

    <div class="morpion-card">

        <!-- Titre -->
        <h1 class="morpion-title">Morpion</h1>

        <p class="morpion-status">
            Joue au morpion en temps réel avec un ami
        </p>

        @if (!string.IsNullOrWhiteSpace(error))
        {
            <div class="alert alert-danger text-center mb-3">
                @error
            </div>
        }

        <!-- Formulaire -->
        <form asp-action="Create" method="post" class="mt-4">

            <div class="form-group mb-3">
                <input name="pseudo"
                       class="form-control form-control-lg text-center"
                       placeholder="Choisir un pseudo..."
                       maxlength="20"
                       autocomplete="off"
                       required />
            </div>

            <button type="submit"
                    class="btn btn-primary btn-lg w-100 rounded-pill">
                Créer un lobby
            </button>

        </form>

        <!-- Infos -->
        <p class="text-muted mt-4 mb-0 small">
            🔗 Un lien sera généré pour inviter un ami à jouer.
        </p>

    </div>

</section>
